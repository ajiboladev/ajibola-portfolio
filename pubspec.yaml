


















name: abdulazeez_portfolio
description: "Portfolio of Abdulazeez Ajibola — Mobile App & Web Developer"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  font_awesome_flutter: ^11.0.0
  url_launcher: ^6.3.0
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # Add your profile image + project screenshots here.
  # Create an `assets/images/` folder and drop files in,
  # then uncomment below:
  # assets:
  #   - assets/images/
